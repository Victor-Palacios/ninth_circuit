name: Extract — OpenRouter (trinity-large)

on:
  schedule:
    - cron: "0 */4 * * *"  # every 4 hours
  workflow_dispatch:

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install \
            supabase==2.28.2 \
            openai==2.28.0 \
            pymupdf==1.27.2 \
            requests==2.32.5 \
            mlflow==3.10.1 \
            psycopg2-binary==2.9.10 \
            sendgrid==6.12.5

      - name: Run extraction
        env:
          PYTHONUNBUFFERED: "1"
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PROVIDER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          PROVIDER_BASE_URL: "https://openrouter.ai/api/v1"
          MODEL: "arcee-ai/trinity-large-preview:free"
          MODEL_LABEL: "arcee-ai/trinity-large-preview:free"
          DATE_FROM: "2021-01-01"
          DATE_TO: "2021-12-31"
        run: python -m pipeline.extract --provider openai --limit 50

      - name: Send email notification
        if: always()
        env:
          SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
          JOB_STATUS: ${{ job.status }}
          JOB_TYPE: "Extract"
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SUMMARY_FILE: extract_summary.txt
        run: python pipeline/notify.py "OpenRouter (trinity 2021)"
