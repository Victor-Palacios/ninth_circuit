name: Classify — OpenRouter (trinity-large, 2021 Nov to 2026)

on:
  schedule:
    - cron: "0 */2 * * *"  # every 2 hours
  workflow_dispatch:

jobs:
  classify:
    permissions:
      contents: write
    uses: ./.github/workflows/_classify_shared.yml
    with:
      provider_name: openrouter
      provider_base_url: "https://openrouter.ai/api/v1"
      model: "arcee-ai/trinity-large-preview:free"
      model_label: "arcee-ai/trinity-large-preview:free"
      date_from: "2021-11-15"
      date_to: "2026-12-31"
      email_subject_label: "OpenRouter (trinity-large 2021-2026)"
    secrets:
      PROVIDER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
